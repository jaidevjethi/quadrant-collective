/**
 * Canonical site origin — used for metadataBase, canonical URLs, schema, and
 * sitemap. Provisional pending domain registration (STRATEGY open decision #1);
 * change here once the domain is confirmed and it propagates everywhere.
 */
export const SITE_URL = "https://quadrantcollective.in";
